#include"main"

/**
 * _strlen - length of a string
 * @s: a pointer given by the main function
 * Return:lenght
 */

int_strlen(char *s)
{

	int lenght = 0;

	while (s[length] != '\0')

	{
		lenght++;
	}
	return (lenght);
}
